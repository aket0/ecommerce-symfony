{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!
{% endblock %}

{% block body %}
	<div class="container">
		<div class="card mb-12">
			<div class="row g-0">
				<div class="col-md-4">
					<img src={{ asset('images/'~produit.image)}} class="img-fluid rounded-start" alt="...">
				</div>
				<div class="col-md-8">
					<div class="card-body">
						<h1 class="card-title">{{produit.nom}}</h1>
						<p class="card-text">{{produit.description}}</p>
						<a href="" class="text-success">Ajouter au panier</a>
					</div>
				</div>
			</div>
		</div>

	</div>
{% endblock %}
